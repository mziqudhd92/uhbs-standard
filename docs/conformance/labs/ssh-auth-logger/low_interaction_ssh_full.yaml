# Formal Low-Interaction SSH TPS (ssh-auth-logger)
target_metadata:
  name: "ssh-auth-logger-ssh"
  class: "Low-Interaction"
  protocol: "ssh"
  protocols: ["ssh"]

performance_baseline:
  expected_p95_latency_ms: 3000
  strict_rfc_enforcement: true
  timing_samples: 1000

safety_boundary:
  allowed_outbound_traffic: false
  allow_local_code_execution: false
