# Advanced tutorial: matrix sensitivity

```bash
uhbs matrix example advanced --out matrix-advanced
uhbs matrix analyze matrix-advanced/matrix-input.json --out report.json
```

Inspect `sensitivity.leave_one_out` to see how the composite moves when each dimension is removed. Correlate with Modules A–F only as *informative* mapping — do not rewrite UHQS weights without a separate RFC and corpus validation.
