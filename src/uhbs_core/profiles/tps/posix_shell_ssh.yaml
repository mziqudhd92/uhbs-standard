target_metadata:
  name: "POSIX-Shell-SSH-Decoy"
  class: "POSIX-Shell"
  protocol: "ssh"
  protocols: ["ssh"]

performance_baseline:
  expected_p95_latency_ms: 3000
  strict_rfc_enforcement: true
  timing_samples: 1000
  # gold_baseline_host: 127.0.0.1   # native SSH/service for KS + HASSH compare

safety_boundary:
  allowed_outbound_traffic: false
  allow_local_code_execution: false
